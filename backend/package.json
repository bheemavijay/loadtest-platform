{
  "name": "loadtest-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Express API for triggering the Go load testing engine",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
